services:
  postgres:
    image: postgres:16-alpine
    container_name: opensource-compass-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: opensource_compass
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./database/schema.sql:/docker-entrypoint-initdb.d/01-schema.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d opensource_compass"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

